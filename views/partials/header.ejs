<header class="main-header">
    
    <!-- 1. NAVEGAÇÃO E LOGO -->
    <div class="nav-container">
        <!-- Menu Mobile (Esquerda) -->
        <div class="mobile-menu-btn">
            <i class="fas fa-bars"></i>
        </div>

        <!-- Logo (Centro) -->
        <div class="logo">
            <a href="/" style="display: flex; align-items: center; gap: 10px; text-decoration: none;">
                <img src="/img/ui/logo.png" alt="Logo" style="height: 60px; width: auto;">
                <!-- O CSS esconde este texto no celular -->
                <span class="desktop-name" style="font-family: 'Playfair Display', serif; font-size: 35px; color: #000; font-weight: 600; letter-spacing: 0.5px; text-transform: uppercase;">
                    Maely Cristina
                </span>
            </a>
        </div>

        <!-- Menu Desktop (Meio) -->
        <nav class="main-nav">
            <ul>
                <li><a href="/">Início</a></li>
                <li><a href="/colecao/lancamentos">Lançamentos</a></li>
                <li><a href="/colecao/vestidos">Vestidos</a></li>
                <li><a href="/colecao/promocao" class="promo-link">Sale</a></li>
                
                <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                    <li style="border-left: 1px solid #ccc; margin-left: 10px; padding-left: 15px;"></li>
                    <li><a href="/admin/pedidos" style="color: #c4a47c; font-weight: bold;">VENDAS</a></li>
                    <li><a href="/admin/produtos" style="color: #c4a47c; font-weight: bold;">PRODUTOS</a></li>
                <% } %>
            </ul>
        </nav>

        <!-- Ícones (Direita) -->
        <div class="user-actions">
            <!-- Busca -->
            <form action="/search" method="GET" class="search-form">
                <input type="text" name="q" placeholder="Buscar...">
                <button type="submit"><i class="fas fa-search"></i></button>
            </form>
            
            <% if (isAuthenticated) { %>
                <!-- Link para Pedidos -->
                <a href="/pedidos" title="Meus Pedidos"><i class="fas fa-user"></i></a>
                
                <!-- BOTÃO DE SAIR (RESTAURADO) -->
                <form action="/logout" method="POST" style="display:inline; margin-left: 5px;">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="btn-logout" title="Sair" style="background:none; border:none; cursor:pointer; font-size:16px;">
                        <i class="fas fa-sign-out-alt"></i>
                    </button>
                </form>
            <% } else { %>
                <a href="/login"><i class="far fa-user"></i></a>
            <% } %>

            <a href="/carrinho" class="cart-icon">
                <i class="fas fa-shopping-bag"></i>
                <span class="badge"><%= typeof cartCount !== 'undefined' ? cartCount : 0 %></span>
            </a>
        </div>
    </div>

    <!-- 2. FAIXA DE FRETE ANIMADA -->
    <div class="top-bar-marquee">
        <div class="marquee-content">
            <span>FRETE GRÁTIS PARA TODO O BRASIL EM COMPRAS ACIMA DE R$ 499,00  •  ENVIAMOS PARA TODO O BRASIL</span>
        </div>
    </div>

</header>